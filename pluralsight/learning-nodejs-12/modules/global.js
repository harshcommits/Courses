// console.dir(global, { depth: 0 }); // prints out the globally defined node objects; eg. setInterval, fetch, setTimeout etc.

// global object can have attributes added to them; not recommended unless absolutely necessary
global.answer = 32;