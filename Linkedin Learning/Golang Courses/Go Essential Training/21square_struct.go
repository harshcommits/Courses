package main

/*
define a square struct with 2 fields; center of type point and length of type int
define following methods:
	1. Move(dx int, dy int)
	2. Area() int
also has following new square definition;
	NewSquare(x int, y int, length int) (*Square, error)
*/

import (
	"fmt"
)

func main() {
	fmt.Println("this is a placeholder till we write actual ")
}
